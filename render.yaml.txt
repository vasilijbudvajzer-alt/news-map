services:
  - type: web
    name: karelia-news-map
    runtime: node
    region: frankfurt
    plan: free
    buildCommand: "npm install"
    startCommand: "npm start"
    envVars:
      - key: NODE_ENV
        value: production